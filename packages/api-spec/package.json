{
  "name": "@app/api-spec",
  "version": "0.1.0",
  "private": true,
  "description": "TypeSpec によるタスク管理 API の契約定義（OpenAPI を出力）",
  "type": "module",
  "scripts": {
    "build": "tsp compile .",
    "watch": "tsp compile . --watch",
    "clean": "rm -rf tsp-output"
  },
  "devDependencies": {
    "@typespec/compiler": "^1.12.0",
    "@typespec/http": "^1.12.0",
    "@typespec/openapi": "^1.12.0",
    "@typespec/openapi3": "^1.12.0",
    "@typespec/rest": "^0.82.0"
  }
}
