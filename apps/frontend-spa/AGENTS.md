# SPA frontend instructions

親ディレクトリの `AGENTS.md` に加え、`apps/frontend-spa/**` を変更する前に `../../.claude/rules/stack-frontend.md` を読んで守ってください。

HTTP 契約を変更する場合は、`packages/api-spec/main.tsp` と `docs/07-api-specification.md` を同じ変更セットで更新し、SPA と SSR の外部から見た機能をそろえます。
